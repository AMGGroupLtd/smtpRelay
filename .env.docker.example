# .env.docker Example
# SMTP relay settings

#SMTP_HOSTNAME
#    name='SMTP_HOSTNAME',
#    default='smtp-relay'  # Hostname the relay binds to


# Docker Settings

#SMTP_DOCKER_NAME
#    name='SMTP_DOCKER_NAME',
#    default='smtp-relay'  # Docker container name

#SMTP_DOCKER_NETWORK
#    name='SMTP_DOCKER_NETWORK',
#    default='smtp'  # Docker network the relay should join


# Public Port Config

#DOCKER_PORT
#    name='DOCKER_PORT',
#    default=None,  # Optional – if defined, the port is published/exposed
#    note='If not exposed, containers must join SMTP_DOCKER_NETWORK to access relay. Ensure this port is NOT publicly accessible to avoid open-relay risk.'
